# Repo Tidy Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Clean repo root — only `README.md`, `CLAUDE.md`, and configs remain; all docs organized under `docs/`. Nothing deleted; `src/` untouched.

**Architecture:** Pure file reorganization via `git mv` (preserves history). Spec filenames kept identical so their cross-references stay valid. Two new top-level docs (README for humans, CLAUDE.md for agents). Verification is command-based (no code tests apply to doc moves), plus a final `npm test`/`npm run build` to prove nothing was disturbed.

**Tech Stack:** git, Markdown.

---

## File Structure

- `docs/specs/` — all `SPEC*.md` (created)
- `docs/guides/` — `how-to-export-cal.md`, `claude-usage-monitor.md` (created)
- `docs/archive/` — stale notes + `telegram-demo-draft/` (created)
- `scripts/fetch-cal.mjs` — moved from root
- `README.md`, `CLAUDE.md` — new at root
- `src/lib/supplements.ts` — one comment edited

---

## Task 1: Move product specs → `docs/specs/`

**Files:** all root `SPEC*.md` (18 files) → `docs/specs/`; edit `src/lib/supplements.ts:64`.

- [ ] **Step 1: Create dir and move via git mv (glob, count-agnostic)**

```bash
mkdir -p docs/specs
git mv SPEC*.md docs/specs/
```

- [ ] **Step 2: Verify all moved, none left in root**

Run: `ls SPEC*.md 2>/dev/null; echo "---"; ls docs/specs | wc -l`
Expected: first `ls` prints nothing (no specs in root); `docs/specs` count = 18.

- [ ] **Step 3: Fix the one code reference (a comment)**

In `src/lib/supplements.ts`, line ~64, change:
```ts
// ── Reminders (SPEC-REMINDERS) ──────────────────────────────────────────────
```
to:
```ts
// ── Reminders (docs/specs/SPEC-REMINDERS.md) ───────────────────────────────
```

- [ ] **Step 4: Commit**

```bash
git add -A docs/specs src/lib/supplements.ts
git commit -m "docs(tidy): move product specs to docs/specs"
```

---

## Task 2: Move guides → `docs/guides/`

**Files:** `HOW_TO_EXPORT_CAL.md`, `usage.md` → `docs/guides/` (renamed).

- [ ] **Step 1: Create dir and move+rename**

```bash
mkdir -p docs/guides
git mv HOW_TO_EXPORT_CAL.md docs/guides/how-to-export-cal.md
git mv usage.md docs/guides/claude-usage-monitor.md
```

- [ ] **Step 2: Verify**

Run: `ls docs/guides`
Expected: `claude-usage-monitor.md  how-to-export-cal.md`

- [ ] **Step 3: Commit**

```bash
git add -A docs/guides
git commit -m "docs(tidy): move how-tos to docs/guides"
```

---

## Task 3: Archive stale notes + telegram-demo draft → `docs/archive/`

**Files:** `new-approch.md`, `new-speca-refactoring.md`, `test-speca.md`, `usage-spec.md`, `TODO_TOMORROW.md` (tracked) + `files-telegram-speca/` (untracked) → `docs/archive/`.

- [ ] **Step 1: Create dir and move tracked notes**

```bash
mkdir -p docs/archive
git mv new-approch.md new-speca-refactoring.md test-speca.md usage-spec.md TODO_TOMORROW.md docs/archive/
```

- [ ] **Step 2: Move the untracked telegram-demo draft folder and stage it**

```bash
mkdir -p docs/archive/telegram-demo-draft
mv files-telegram-speca/* docs/archive/telegram-demo-draft/
rmdir files-telegram-speca
git add docs/archive/telegram-demo-draft
```

- [ ] **Step 3: Verify**

Run: `ls docs/archive; echo "---"; ls files-telegram-speca 2>/dev/null || echo "(gone)"`
Expected: archive lists the 5 notes + `telegram-demo-draft/`; `files-telegram-speca` is gone.

- [ ] **Step 4: Commit**

```bash
git add -A docs/archive
git commit -m "docs(tidy): archive stale notes and telegram-demo draft"
```

---

## Task 4: Move `fetch-cal.mjs` → `scripts/`

**Files:** `fetch-cal.mjs` → `scripts/fetch-cal.mjs`.

- [ ] **Step 1: Move**

```bash
git mv fetch-cal.mjs scripts/fetch-cal.mjs
```

- [ ] **Step 2: Verify no code/config imports it from root path**

Run: `grep -rn "fetch-cal" package.json src 2>/dev/null || echo "(no refs)"`
Expected: `(no refs)` (it is a standalone manual script).

- [ ] **Step 3: Commit**

```bash
git add -A
git commit -m "chore(tidy): move fetch-cal.mjs to scripts/"
```

---

## Task 5: Create `README.md`

**Files:** Create `README.md`.

- [ ] **Step 1: Write the file**

````markdown
# Tonus

Личный health-хаб: подключаешь Apple Watch, логируешь привычки и анализы, а AI
находит закономерности, которые влияют на самочувствие.

## Стек

- **Frontend:** React 19 + Vite 8 + TypeScript → Vercel
- **Backend:** Supabase (Postgres + Edge Functions на Deno)
- **AI:** Gemini 2.5 Flash · **Бот:** Telegram

## Быстрый старт

> ⚠️ Vite 8 требует **Node ≥ 20.19 / 22.12**. Дефолтный Node 18 падает
> (`CustomEvent is not defined`) — используй Node 24 (`nvm use 24`).

```bash
npm install
npm run dev      # дев-сервер (нужен .env с VITE_SUPABASE_URL / VITE_SUPABASE_ANON_KEY)
npm test         # vitest
npm run build    # tsc + vite build
```

## Структура

- `src/` — фронтенд (компоненты по фичам, `lib/`, `hooks/`, `parsers/`, `store/`)
- `supabase/` — миграции и edge-функции
- `docs/specs/` — продуктовые спеки (фазы 3–10, фичи)
- `docs/guides/` — гайды (экспорт календаря, монитор использования Claude)
- `docs/superpowers/` — спеки и планы из superpowers-воркфлоу
- `docs/archive/` — устаревшие черновики
- `scripts/` — вспомогательные скрипты
- `claude-monitor/` — сервис мониторинга лимитов Claude (launchd)

## Деплой

Фронтенд авто-деплоится на Vercel при push в `main`. Edge-функции — отдельно
(`npx supabase functions deploy <name> --project-ref <ref>`). Подробнее — [CLAUDE.md](CLAUDE.md).
````

- [ ] **Step 2: Commit**

```bash
git add README.md
git commit -m "docs: add README"
```

---

## Task 6: Create `CLAUDE.md`

**Files:** Create `CLAUDE.md`.

- [ ] **Step 1: Write the file**

````markdown
# CLAUDE.md

Гайд для агентов (Claude Code) по этому репозиторию.

## Команды

- **Dev/build требуют Node 24** (Vite 8 требует ≥20.19/22.12; дефолтный Node 18
  падает с `CustomEvent is not defined`): `nvm use 24` или
  `export PATH="$HOME/.nvm/versions/node/v24.16.0/bin:$PATH"`.
- `npm test` — vitest (работает и на Node 18). Окружение **node**, не jsdom:
  рендер React-компонентов недоступен. Паттерн теста компонента — проверка
  экспорта + покрытие переводов (см. `src/components/auth/TelegramDemo.test.ts`);
  чистую логику тестируй напрямую.
- `npm run build` — `tsc -b && vite build`.
- `npm run lint` — eslint (в проекте есть pre-existing ошибки; не добавляй новых).

## Локальный запуск

Нет локального `.env` (прод берёт ключи из Vercel). Для `npm run dev` создай
временный `.env.local` (gitignored):

```
VITE_SUPABASE_URL=http://localhost:54321
VITE_SUPABASE_ANON_KEY=test-anon-key
```

Лендинг полностью статичный, dummy-ключей хватает, чтобы его открыть.

## Деплой

- **Фронтенд:** авто-деплой Vercel при push в `main` (репо `github.com/batstolya/tonus`).
- **Edge-функции:** не деплоятся через Vercel-пайплайн. Отдельно:
  `npx supabase functions deploy <name> --project-ref <ref>`. Логин CLI — в macOS Keychain.

## Конвенции

- Продуктовые спеки — `docs/specs/`. Гайды — `docs/guides/`.
- Superpowers-артефакты (design/plan) — `docs/superpowers/{specs,plans}/`.
- Устаревшие черновики — `docs/archive/` (не удаляем).
- `src/` — компоненты по фичам (`components/<feature>/`), общая логика в `lib/`.
````

- [ ] **Step 2: Commit**

```bash
git add CLAUDE.md
git commit -m "docs: add CLAUDE.md agent guide"
```

---

## Task 7: Final verification

- [ ] **Step 1: Root has no stray markdown**

Run: `ls *.md`
Expected: exactly `CLAUDE.md  README.md` (nothing else).

- [ ] **Step 2: No broken intra-doc spec cross-references**

Run: `grep -rIoE "SPEC-[A-Z0-9-]+\.md" docs/specs | awk -F: '{print $2}' | sort -u | while read f; do test -e "docs/specs/$f" || echo "MISSING: $f"; done; echo "checked"`
Expected: only `checked` (no `MISSING:` lines).

- [ ] **Step 3: Nothing in the codebase was disturbed**

Run: `export PATH="$HOME/.nvm/versions/node/v24.16.0/bin:$PATH" && npm test && npm run build`
Expected: 56/56 tests pass; `✓ built`.

- [ ] **Step 4: Working tree clean (only intended changes)**

Run: `git status --short`
Expected: clean (everything committed; only the unrelated `claude-monitor/data/*` runtime files may remain modified).

---

## Self-Review (done at authoring)

- **Spec coverage:** §1 structure → Tasks 1–6; §2 moves table → Tasks 1–4; §3 new files → Tasks 5–6; §4 ref fix → Task 1 Step 3; §5 untouched → nothing in plan touches `src/` except the one comment; §6 verification → Task 7. No gaps.
- **Placeholders:** none — every move has an exact `git mv`, both new docs have full content.
- **Consistency:** destination paths in tasks match the spec's structure tree; `SPEC*.md` glob covers all specs regardless of exact count.
