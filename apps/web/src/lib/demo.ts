// Facade: demo mode is cross-client — the mobile app gets the same
// backend-free workbench. The implementation lives in @tonus/shared.
export { isDemoActive, enableDemo, disableDemo } from '@tonus/shared'
